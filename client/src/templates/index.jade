doctype html
html(lang="en")
	head
		meta(charset="utf-8")
		title 3DBox
	body
		.well.well-sm
			form.form-inline(name="controlsForm" id="controlsForm")
				.form-group
					label.sr-only(for="length") Length
					input.form-control(type="number" id="length" name="boxLength" placeholder="length" value="10")
				.form-group
					label.sr-only(for="width") Width
					input.form-control(type="number" id="width" name="boxWidth" placeholder="width" value="10")
				.form-group
					label.sr-only(for="height") Height
					input.form-control(type="number" id="height" name="boxHeight" placeholder="height" value="10")
				button.btn.btn-success(type="submit") Send request